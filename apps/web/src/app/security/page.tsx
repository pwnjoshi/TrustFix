import { CheckCircle, Key, LockKey, ShieldCheck, UsersThree } from "@phosphor-icons/react/dist/ssr";
import { MarketingFooter, MarketingHeader } from "@/components/marketing-shell";

export default function SecurityPage() { return <main className="marketing-page"><MarketingHeader/><section className="inner-hero"><span className="section-index">SECURITY</span><h1>Autonomy with explicit boundaries.</h1><p>TrustFix is designed as security infrastructure: separated identities, tenant-scoped records, authenticated access, approval gates, drift checks, and proof after every change.</p></section><section className="security-principles">{[[Key,"Dedicated identities","Web, API, scanner, and remediator services use separate Cloud Run identities."],[LockKey,"Least privilege","The scanner is read-oriented. Storage mutation is scoped to the disposable demo bucket."],[UsersThree,"Workspace authorization","Google IAP authenticates users; backend role checks enforce Owner, Admin, Reviewer, and Viewer permissions."],[ShieldCheck,"Governed changes","Sensitive changes require an approval record and idempotency key. Unexpected drift aborts execution."],[CheckCircle,"Evidence before answers","A control becomes verified only after the measurable property passes again."]].map(([Icon,title,copy])=><article key={String(title)}><Icon/><div><h2>{String(title)}</h2><p>{String(copy)}</p></div></article>)}</section><section className="boundary-table"><div><span className="section-index">CURRENT SUPPORT BOUNDARY</span><h2>What the deployed build does today.</h2></div><div><p><strong>Automatic observation</strong>Storage IAM, Cloud Run IAM, and internet-exposed administrative firewall ports.</p><p><strong>Live governed mutation</strong>Public-access removal for the explicitly named disposable TrustFix bucket.</p><p><strong>Approval-only planning</strong>Cloud Run and firewall findings remain inspectable, but mutation executors stay disabled until dedicated rollback acceptance tests pass.</p><p><strong>Never claimed</strong>Drive, Gmail, Slack, HR, training, and policy-document evidence without a connected source.</p></div></section><MarketingFooter/></main>; }

import { ArrowRight, CheckCircle, Fingerprint, FlowArrow, ShieldCheck } from "@phosphor-icons/react/dist/ssr";
import Link from "next/link";
import { MarketingFooter, MarketingHeader } from "@/components/marketing-shell";

export default function ProductPage() { return <main className="marketing-page"><MarketingHeader/><section className="inner-hero"><span className="section-index">PRODUCT</span><h1>Security review operations, grounded in infrastructure.</h1><p>TrustFix converts questionnaire requirements into measurable controls, inspects Google Cloud, governs the smallest safe remediation, and attaches proof to the final answer.</p><div className="hero-actions"><Link className="button primary" href="/app">Open real workspace <ArrowRight/></Link><Link className="button secondary" href="/demo">Try public demo</Link></div></section><section className="detail-grid">{[[FlowArrow,"Review orchestration","Import a questionnaire, map supported requirements, coordinate asynchronous scans, and preserve every status transition."],[Fingerprint,"Evidence records","Keep the source, project, resource identifier, relevant properties, collection time, and verification state."],[ShieldCheck,"Governed remediation","Show the current state, exact delta, impact, rollback, risk, and policy decision before execution."],[CheckCircle,"Independent verification","Re-run the original control and an external probe where practical. API success alone never means verified."]].map(([Icon,title,copy]) => <article key={String(title)}><Icon size={24}/><h2>{String(title)}</h2><p>{String(copy)}</p></article>)}</section><section className="product-workflow"><div><span className="section-index">THE OPERATING LOOP</span><h2>From question to defensible answer.</h2></div><ol>{["Interpret the natural-language requirement","Map it to a deterministic control","Inspect the disposable target project","Collect resource-specific evidence","Evaluate pass, fail, or incomplete","Plan the minimum safe remediation","Apply policy and human approval","Execute with drift protection","Re-test the security property","Export the verified answer and audit trail"].map((item,index)=><li key={item}><span>{String(index+1).padStart(2,"0")}</span>{item}</li>)}</ol></section><MarketingFooter/></main>; }
